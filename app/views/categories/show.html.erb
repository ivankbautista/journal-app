<h1><%= @category.title %></h1>

<ul>
    <li><%= link_to "Edit", edit_category_path(@category) %></li>
    <li><%= link_to "Remove Entry", category_path(@category),
                    method: :delete,
                    data: { confirm: 'Are you sure with this?' } %></li>
</ul>

<h2>List of Tasks</h2>
    <%= render @category.tasks %>

<h2>Add a task:</h2>
    <%= render 'tasks/form' %>