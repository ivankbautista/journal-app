<div class="w-full h-full">
    <h1 class="text-4xl mb-10 mx-3 text-center text-black">🌤️Your Tasks Today:</h1>

    <div class="flex flex-wrap">
        <% @categories.each do |category| %>
            <%= render 'categorycard', category: category %>
        <% end %>
    </div>

</div>